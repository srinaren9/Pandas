#Program to access individual element having duplicate indexes
import pandas as pd
import numpy as np
a=np.arange(2.75,50,9.75)
print(a)
obj=pd.Series(a,index=['a','b','a','a','b'])
print(obj)
print("To display element with duplicate indexes")
print(obj['a'])
print("To display element with duplicate indexes")
print(obj['b'])
